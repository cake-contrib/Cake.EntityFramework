PostgreSQL:
  container_name: postgresql-cake
  image: sameersbn/postgresql:9.4-22
  ports:
    - "5432:5432"
  environment:
    - DEBUG=false
    - PG_TRUST_LOCALNET=true
    - PG_PASSWORD=Password12! # Match CI DB config - https://www.appveyor.com/docs/services-databases